<h1>{{#if _id}}Edit{{else}}Add{{/if}} Todo</h1>

<form action="/todo/{{_id}}" method="POST">
  <input type="text" name="title" value="{{title}}" placeholder="Todo Title"><br/>
  <input type="submit" class="btn btn-primary btn-lg"
  value="{{#if _id}}Edit{{else}}Add{{/if}} Todo"/>
</form>
